import styled from 'styled-components';

import { spacing } from 'src/core';

export const TopNavigationContainerDiv = styled.div`
  padding-top: ${spacing.four};
  width: 100%;
  display: flex;
  min-height: 100px;
`;
TopNavigationContainerDiv.displayName = 'TopNavigationContainerDiv';

import { routes } from '../routes';

const initialState = {
  routes,
};

export const domainsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

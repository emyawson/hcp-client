import styled from 'styled-components';

import { TableHeader } from 'src/domains/diagnostics/components';

export const LogbookTableHeader = styled(TableHeader)`
  display: flex;
  flex-direction: column;
`;

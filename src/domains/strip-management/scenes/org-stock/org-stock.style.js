import styled from 'styled-components';

import { spacing } from 'src/core';

export const InnerWrapper = styled.div`
  width: 100%;
`;

export const OrgStockInputDiv = styled.div`
  margin: 0 auto ${spacing.three};
`;

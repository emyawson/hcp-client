import * as Dropdown from './dropdown-chain.component';

describe('Profile dropdown container test suite ', () => {
  it('should have these functions defined', () => {
    expect(Dropdown.DropdownChain).toBeDefined();
  });
});
